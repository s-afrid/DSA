#include<bits/stdc++.h>
using namespace std;
int main() {
    // string is a collection of characters same as array
    string s = "Hello";
    cout << s[1] << endl; // Access element by index
    // to find the length of the string
    cout << s.size() << endl;
    cout << s.length() << endl;
}